{
  "name": "xfi-token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ gruberx/xfi-token",
    "create-local": "graph create --node http://localhost:8020/ gruberx/xfi-token",
    "remove-local": "graph remove --node http://localhost:8020/ gruberx/xfi-token",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gruberx/xfi-token"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
